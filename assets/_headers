# Workers Static Assets serves this directory verbatim (vite publicDir).
# Vite emits content-hashed filenames under /assets/*, so they are safe to
# cache forever; the HTML shell keeps the default revalidation so deploys
# propagate immediately.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
